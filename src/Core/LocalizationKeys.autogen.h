#pragma once
// Auto-generated by tools/generate_localization_bindings.py. Do not edit by hand.
// Source: resource/localization/Localization.resx
#include <string>

struct LocalizationKeysAccessor
{
        const std::string& Get(const std::string& key) const;

        // Toggle me with %s
        inline const std::string& Toggle_me_with_s() const { return Get("Toggle me with %s"); }

        // Toggle Online with %s
        inline const std::string& Toggle_Online_with_s() const { return Get("Toggle Online with %s"); }

        // Toggle HUD with %s
        inline const std::string& Toggle_HUD_with_s() const { return Get("Toggle HUD with %s"); }

        // Generate Debug Logs
        inline const std::string& Generate_Debug_Logs() const { return Get("Generate Debug Logs"); }

        // Write DEBUG.txt with detailed runtime information. Saved to settings.ini for future sessions.
        inline const std::string& Write_DEBUG_txt_with_detailed_runtime_information_Saved_to_settings_ini_for_future_sessions() const { return Get("Write DEBUG.txt with detailed runtime information. Saved to settings.ini for future sessions."); }

        // Online
        inline const std::string& Online() const { return Get("Online"); }

        // Gameplay settings
        inline const std::string& Gameplay_settings() const { return Get("Gameplay settings"); }

        // Custom palettes
        inline const std::string& Custom_palettes() const { return Get("Custom palettes"); }

        // Hitbox overlay
        inline const std::string& Hitbox_overlay() const { return Get("Hitbox overlay"); }

        // FrameHistory
        inline const std::string& FrameHistory() const { return Get("FrameHistory"); }

        // Framedata
        inline const std::string& Framedata() const { return Get("Framedata"); }

        // Avatar settings
        inline const std::string& Avatar_settings() const { return Get("Avatar settings"); }

        // Controller Settings
        inline const std::string& Controller_Settings() const { return Get("Controller Settings"); }

        // Loaded settings.ini values
        inline const std::string& Loaded_settings_ini_values() const { return Get("Loaded settings.ini values"); }

        // Current online players:
        inline const std::string& Current_online_players() const { return Get("Current online players:"); }

        // <No data>
        inline const std::string& No_data() const { return Get("<No data>"); }

        // Avatar
        inline const std::string& Avatar() const { return Get("Avatar"); }

        // Color
        inline const std::string& Color() const { return Get("Color"); }

        // Accessory 1
        inline const std::string& Accessory_1() const { return Get("Accessory 1"); }

        // Accessory 2
        inline const std::string& Accessory_2() const { return Get("Accessory 2"); }

        // CONNECT TO NETWORK MODE FIRST
        inline const std::string& CONNECT_TO_NETWORK_MODE_FIRST() const { return Get("CONNECT TO NETWORK MODE FIRST"); }

        // FrameHistory help
        inline const std::string& FrameHistory_help() const { return Get("FrameHistory help"); }

        // FrameHistory auto reset help
        inline const std::string& FrameHistory_auto_reset_help() const { return Get("FrameHistory auto reset help"); }

        // Box width
        inline const std::string& Box_width() const { return Get("Box width"); }

        // Box height
        inline const std::string& Box_height() const { return Get("Box height"); }

        // spacing
        inline const std::string& spacing() const { return Get("spacing"); }

        // Enable##framehistory_section
        inline const std::string& Enable_framehistory_section() const { return Get("Enable##framehistory_section"); }

        // Auto Reset##Reset after each idle frame
        inline const std::string& Auto_Reset_Reset_after_each_idle_frame() const { return Get("Auto Reset##Reset after each idle frame"); }

        // Enable##framedata_section
        inline const std::string& Enable_framedata_section() const { return Get("Enable##framedata_section"); }

        // Advantage on stagger hit
        inline const std::string& Advantage_on_stagger_hit() const { return Get("Advantage on stagger hit"); }

        // YOU ARE NOT IN MATCH!
        inline const std::string& YOU_ARE_NOT_IN_MATCH() const { return Get("YOU ARE NOT IN MATCH!"); }

        // YOU ARE NOT IN TRAINING MODE OR REPLAY THEATER!
        inline const std::string& YOU_ARE_NOT_IN_TRAINING_MODE_OR_REPLAY_THEATER() const { return Get("YOU ARE NOT IN TRAINING MODE OR REPLAY THEATER!"); }

        // YOU ARE NOT IN A MATCH, IN TRAINING MODE OR REPLAY THEATER!
        inline const std::string& YOU_ARE_NOT_IN_A_MATCH_IN_TRAINING_MODE_OR_REPLAY_THEATER() const { return Get("YOU ARE NOT IN A MATCH, IN TRAINING MODE OR REPLAY THEATER!"); }

        // THERE WAS AN ERROR LOADING ONE/BOTH OF THE CHARACTERS
        inline const std::string& THERE_WAS_AN_ERROR_LOADING_ONE_BOTH_OF_THE_CHARACTERS() const { return Get("THERE WAS AN ERROR LOADING ONE/BOTH OF THE CHARACTERS"); }

        // Palette editor
        inline const std::string& Palette_editor() const { return Get("Palette editor"); }

        // YOU ARE NOT IN MATCH! (Replay)
        inline const std::string& YOU_ARE_NOT_IN_MATCH_Replay() const { return Get("YOU ARE NOT IN MATCH! (Replay)"); }

        // YOU ARE NOT IN REPLAY MENU!
        inline const std::string& YOU_ARE_NOT_IN_REPLAY_MENU() const { return Get("YOU ARE NOT IN REPLAY MENU!"); }

        // YOU ARE NOT IN CHARACTER SELECTION!
        inline const std::string& YOU_ARE_NOT_IN_CHARACTER_SELECTION() const { return Get("YOU ARE NOT IN CHARACTER SELECTION!"); }

        // Hide HUD
        inline const std::string& Hide_HUD() const { return Get("Hide HUD"); }

        // Toggle Rewind
        inline const std::string& Toggle_Rewind() const { return Get("Toggle Rewind"); }

        // Replay Database
        inline const std::string& Replay_Database() const { return Get("Replay Database"); }

        // Enable/Disable Upload
        inline const std::string& Enable_Disable_Upload() const { return Get("Enable/Disable Upload"); }

        // Discord
        inline const std::string& Discord() const { return Get("Discord"); }

        // Forum
        inline const std::string& Forum() const { return Get("Forum"); }

        // GitHub
        inline const std::string& GitHub() const { return Get("GitHub"); }

        // Player1
        inline const std::string& Player1() const { return Get("Player1"); }

        // Player2
        inline const std::string& Player2() const { return Get("Player2"); }

        // Draw hitbox/hurtbox
        inline const std::string& Draw_hitbox_hurtbox() const { return Get("Draw hitbox/hurtbox"); }

        // Draw origin
        inline const std::string& Draw_origin() const { return Get("Draw origin"); }

        // Draw collision
        inline const std::string& Draw_collision() const { return Get("Draw collision"); }

        // Draw throw/range boxes
        inline const std::string& Draw_throw_range_boxes() const { return Get("Draw throw/range boxes"); }

        // The point in space where your character resides. 
Important!: This is a single point, the render is composed of two lines to facilitate viewing, the actual point is where the two lines touch.
        inline const std::string& The_point_in_space_where_your_character_resides_Important_This_is_a_single_point_the_render_is_composed_of_two_lines_to_facilitate_viewing_the_actual_point_is_where_the_two_lines_touch() const { return Get("The point in space where your character resides. 
Important!: This is a single point, the render is composed of two lines to facilitate viewing, the actual point is where the two lines touch."); }

        // Defines collision between objects/characters. Also used for throw range checks.
        inline const std::string& Defines_collision_between_objects_characters_Also_used_for_throw_range_checks() const { return Get("Defines collision between objects/characters. Also used for throw range checks."); }

        // Throw range help
        inline const std::string& Throw_range_help() const { return Get("Throw range help"); }

        // Freeze frame:
        inline const std::string& Freeze_frame() const { return Get("Freeze frame:"); }

        // Frame Count
        inline const std::string& Frame_Count() const { return Get("Frame Count"); }

        // Reset
        inline const std::string& Reset() const { return Get("Reset"); }

        // Freeze animation
        inline const std::string& Freeze_animation() const { return Get("Freeze animation"); }

        // Stop timer
        inline const std::string& Stop_timer() const { return Get("Stop timer"); }

        // Step frames
        inline const std::string& Step_frames() const { return Get("Step frames"); }

        // YOU ARE NOT IN TRAINING, VERSUS, OR REPLAY!
        inline const std::string& YOU_ARE_NOT_IN_TRAINING_VERSUS_OR_REPLAY() const { return Get("YOU ARE NOT IN TRAINING, VERSUS, OR REPLAY!"); }

        // Game Mode
        inline const std::string& Game_Mode() const { return Get("Game Mode"); }

        // Enable##hitbox_overlay_section
        inline const std::string& Enable_hitbox_overlay_section() const { return Get("Enable##hitbox_overlay_section"); }

        // Custom palette reload
        inline const std::string& Custom_palette_reload() const { return Get("Custom palette reload"); }

        // Enable##framehistory_section checkbox
        inline const std::string& Enable_framehistory_section_checkbox() const { return Get("Enable##framehistory_section checkbox"); }

        // Draw hitbox/hurtbox##hitbox_overlay_section
        inline const std::string& Draw_hitbox_hurtbox_hitbox_overlay_section() const { return Get("Draw hitbox/hurtbox##hitbox_overlay_section"); }

        // Draw origin##hitbox_overlay_section
        inline const std::string& Draw_origin_hitbox_overlay_section() const { return Get("Draw origin##hitbox_overlay_section"); }

        // Draw collision##hitbox_overlay_section
        inline const std::string& Draw_collision_hitbox_overlay_section() const { return Get("Draw collision##hitbox_overlay_section"); }

        // Step frames##hitbox_overlay_section
        inline const std::string& Step_frames_hitbox_overlay_section() const { return Get("Step frames##hitbox_overlay_section"); }

        // Gameplay settings header
        inline const std::string& Gameplay_settings_header() const { return Get("Gameplay settings header"); }

        // Hide HUD checkbox
        inline const std::string& Hide_HUD_checkbox() const { return Get("Hide HUD checkbox"); }

        // States
        inline const std::string& States() const { return Get("States"); }

        // Log
        inline const std::string& Log() const { return Get("Log"); }

        // Replay Rewind
        inline const std::string& Replay_Rewind() const { return Get("Replay Rewind"); }

        // Replay Upload
        inline const std::string& Replay_Upload() const { return Get("Replay Upload"); }

        // Enable##framehistory_section label
        inline const std::string& Enable_framehistory_section_label() const { return Get("Enable##framehistory_section label"); }

        // Auto Reset label
        inline const std::string& Auto_Reset_label() const { return Get("Auto Reset label"); }

        // FrameHistory width label
        inline const std::string& FrameHistory_width_label() const { return Get("FrameHistory width label"); }

        // FrameHistory height label
        inline const std::string& FrameHistory_height_label() const { return Get("FrameHistory height label"); }

        // FrameHistory spacing label
        inline const std::string& FrameHistory_spacing_label() const { return Get("FrameHistory spacing label"); }

        // FrameHistory not enabled message
        inline const std::string& FrameHistory_not_enabled_message() const { return Get("FrameHistory not enabled message"); }

        // FrameHistory character load error
        inline const std::string& FrameHistory_character_load_error() const { return Get("FrameHistory character load error"); }

        // Language
        inline const std::string& Language() const { return Get("Language"); }

        // Language incomplete label
        inline const std::string& Language_incomplete_label() const { return Get("Language incomplete label"); }

        // Language selection help
        inline const std::string& Language_selection_help() const { return Get("Language selection help"); }
};

